const styles = theme => ({
  textField: {
    marginLeft: theme.spacing(1),
    marginRight: theme.spacing(1),
    width: 200
  },
  form: {
    justifyContent: 'center',
    display: 'flex',
    marginLeft: 20,
    marginRight: 20,
    marginTop: 100
  },

  container: {
    marginBottom: 56,
    padding: 0
  }
});

export default styles;
